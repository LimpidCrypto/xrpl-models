{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/LimpidCrypto/xrpl-models/main/schemas/ledger/base_ledger_entry.json",
    "title": "BaseLedgerEntry",
    "type": "object",
    "properties": {
        "index": {
            "$ref": "https://raw.githubusercontent.com/LimpidCrypto/xrpl-models/main/schemas/types.json#/$defs/HASH256",
            "description": "The unique ID for this ledger entry. In JSON, this field is represented with different names depending on the context and API method. (Note, even though this is specified as \"optional\" in the code, every ledger entry should have one unless it's legacy data from very early in the XRP Ledger's history.)"
        }
    }
}
