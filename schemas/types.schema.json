{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/LimpidCrypto/xrpl-models/main/schemas/types.schema.json",
    "$defs": {
        "HASH256": {
            "type": "string",
            "description": "A 256-bit hash that can be used to identify a ledger object.",
            "pattern": "^[A-F0-9]{64}$"
        }
    }
}
